"""Legacy flat rule projector — replaced by projectors.sources.flat + projectors.targets.

This module exists only to avoid breaking imports during transition.
Use Projection(FlatRuleSource, target) instead.
"""
